server:
  port: 9090
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  profiles:
    include: oauth
  config:
    import: classpath:application-api.yml, classpath:application-oauth.yml, classpath:application-swagger.yml
  thymeleaf:
    cache: false
  datasource:
    url: jdbc:mysql://localhost:3306/AWS
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root # .env 파일에서 값 가져오기
    password: khs04140 # .env 파일에서 값 가져오기

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        format_sql: true
        show_sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect


logging:
  level:
    org:
      hibernate:
        SQL: debug
        type: trace
